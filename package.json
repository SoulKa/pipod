{
  "name": "piPod",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*",
    "standalone/*"
  ],
  "engines": {
    "node": "^22.18.0 || >=24.12.0"
  },
  "scripts": {
    "dev:board": "yarn workspace @pipod/board dev",
    "dev:console": "yarn workspace @pipod/console dev",
    "dev:server": "yarn workspace @pipod/server dev",
    "dev:launcher": "yarn workspace @pipod/launcher dev",
    "dev:dashboard": "yarn workspace @pipod/dashboard dev",
    "preview:board": "yarn workspace @pipod/board preview",
    "preview:console": "yarn workspace @pipod/console preview",
    "start:server": "yarn workspace @pipod/server start",
    "build:launcher": "yarn workspace @pipod/launcher build",
    "prepare-seed": "yarn workspace @pipod/board build-only && yarn workspace @pipod/dashboard build-only && OUT_DIR=standalone/launcher/resources/seed node scripts/build-manifest.mjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "build": "yarn type-check && yarn workspace @pipod/board build-only && yarn workspace @pipod/console build-only",
    "type-check": "yarn workspace @pipod/shared type-check && yarn workspace @pipod/server type-check && yarn workspace @pipod/board type-check && yarn workspace @pipod/console type-check && yarn workspace @pipod/dashboard type-check && yarn workspace @pipod/launcher typecheck",
    "format": "prettier --write . --ignore-unknown",
    "format:check": "prettier --check . --ignore-unknown"
  },
  "devDependencies": {
    "@vue/compiler-dom": "3.5.41",
    "@vue/test-utils": "2.4.11",
    "happy-dom": "20.11.2",
    "prettier": "3.9.6",
    "vitest": "4.1.10",
    "vue": "3.5.41"
  },
  "packageManager": "yarn@4.18.0"
}
